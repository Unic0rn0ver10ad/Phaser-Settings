/**
 * App-owned typography: the playground loads fonts (see index.html) and uses this
 * constant for the settings modal theme and its own UI. Aligns with INTEGRATION.md:
 * "App owns font files/import timing; library accepts font choices through theme."
 */
export const APP_FONT_FAMILY = 'Nunito, sans-serif';
